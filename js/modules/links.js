import {getLinks} from '../api.js';export async function initLinks(){const container=document.getElementById('linksContainer');container.innerHTML='Loading...';try{const data=await getLinks();container.innerHTML='';data.links.forEach(link=>{const card=document.createElement('a');card.className='link-card';card.href=link.url;card.target='_blank';card.innerHTML=`<div>${link.icon||'🔗'}</div><div><div>${link.nama}</div><div>${link.deskripsi||''}</div></div>`;container.appendChild(card);});}catch(err){container.innerHTML='Failed connect API';console.error(err);}}